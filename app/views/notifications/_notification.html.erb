<% if !current_user.user_notifications.blank? %>
	<%= render partial: 'notifications/show', collection: current_user.user_notifications , as: :userNotification%>
<% else %>
	You have no notification
<% end %>

