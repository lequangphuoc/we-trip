<% if !current_user.user_notifications.blank? %>
    <%= render partial: 'notifications/show', collection: current_user.user_notifications, as: :user_notification %>
<% else %>
    You have no notification
<% end %>
